repos:
- repo: local
  hooks:
    - id: sqlx-prepare-check
      files: '^backend/(.*)'
      name: Check if the prepared queries are correct
      entry: task db:check
      language: system
      pass_filenames: false

